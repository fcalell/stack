// ─── Shared style constants ───

// The hairline look. `Separator` renders it, and item's and sidebar's
// internal hairlines compose the same base, so the look lives once and no
// class crosses a component boundary.
export const separatorClass = "h-px w-full shrink-0 border-0 bg-edge";
