// The style channels no component takes, declared `?: never` on every props
// type: the four the canon closes on both platforms, plus the per-prop class
// channels uniwind adds to React Native's hosts, which would reopen the look
// one property at a time.
export interface Closed {
	class?: never;
	className?: never;
	classList?: never;
	style?: never;
	colorClassName?: never;
	selectionColorClassName?: never;
	selectionHandleColorClassName?: never;
	placeholderTextColorClassName?: never;
	cursorColorClassName?: never;
	underlineColorAndroidClassName?: never;
}
